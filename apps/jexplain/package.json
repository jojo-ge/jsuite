{
  "name": "jexplain",
  "private": true,
  "type": "module",
  "description": "Blog-style explainers authored by Claude — the reading shell over the shared @jsuite/documents block system, with live editable charts shared with jChart and per-block notes that flow back to Claude.",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare && node ../../packages/charting/scripts/copy-excalidraw-assets.mjs"
  },
  "dependencies": {
    "@iconify-json/lucide": "^1.2.0",
    "@jsuite/data": "workspace:*",
    "@jsuite/documents": "workspace:*",
    "@nuxt/ui": "^4.10.0",
    "nuxt": "^4.5.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  }
}
