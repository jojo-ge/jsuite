<script setup lang="ts">
// jDiff is the review app, so its home page IS the repo picker — the same
// <DiffHome> the layer serves at /diffs, which every other consumer gets. The
// name over it comes from `diff.brand` in app.config.ts.
</script>

<template>
  <DiffHome />
</template>
