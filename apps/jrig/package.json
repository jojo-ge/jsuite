{
  "name": "jrig",
  "private": true,
  "type": "module",
  "description": "Avatar studio — draw, rig and keyframe 2D characters as live vector documents over one shared skeleton; every clip plays on every character.",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest run"
  },
  "dependencies": {
    "@iconify-json/lucide": "^1.2.0",
    "@jsuite/data": "workspace:*",
    "@nuxt/ui": "^4.10.0",
    "nuxt": "^4.5.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@vue/test-utils": "^2.4.11",
    "happy-dom": "^20.11.0",
    "vitest": "^4.1.0"
  }
}
