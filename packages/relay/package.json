{
  "name": "@jsuite/relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "jTicket sync relay: the Supabase Realtime deploy wizard, plus a local broadcast server that stands in for it in tests.",
  "main": "./index.mjs",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "default": "./index.mjs"
    },
    "./package.json": "./package.json"
  },
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
